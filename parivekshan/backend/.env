# Parivekshan AI - Backend Environment Configuration
# PostgreSQL connection

PGHOST=localhost
PGPORT=5432
PGDATABASE=parivekshan
PGUSER=parivekshan
PGPASSWORD=parivekshan

# Server
PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Ensure the schema is applied on boot (recommended for dev)
DB_AUTO_SCHEMA=false
