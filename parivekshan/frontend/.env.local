# Local override — takes priority over .env
# Node backend (projects, stats, alerts)
VITE_API_URL=http://127.0.0.1:5000
# Python ML model server (XGBoost predictions) — running on 8000, not 8001
VITE_ML_URL=http://127.0.0.1:8000
